# ⚡ Быстрый старт (Групповой режим)

## 🎯 Сверх-простая установка - за 3 минуты!

### Шаг 1: Запустите бота (он уже создан!)

Ваш бот: **@checklist_IELTS_account_bot**

```bash
./start.sh
```

Или с Docker:
```bash
docker-compose up -d
```

### Шаг 2: Добавьте бота в группу

1. **Создайте группу** в Telegram с вашими study buddies
2. **Добавьте бота** @checklist_IELTS_account_bot в группу
3. **Отправьте** `/start` в группе

### Шаг 3: Получите ID группы (для напоминаний)

1. Откройте логи бота:
```bash
docker-compose logs
```

2. Найдите строку типа:
```
Message received from chat_id=-1001234567890, type=group, title=IELTS Study Group
```

3. Скопируйте число после `chat_id=` (включая минус!)

4. Откройте файл `.env`:
```bash
nano .env
```

5. Вставьте ID:
```env
GROUP_CHAT_ID=-1001234567890
```

6. Перезапустите бота:
```bash
docker-compose restart
```

## ✅ Готово!

Теперь:
- Бот будет присылать напоминания в группу в **9:00** и **14:30**
- Каждый может использовать `/today` чтобы отметить свои задачи
- Используйте `/all` чтобы увидеть прогресс всех
- Бот автоматически регистрирует всех кто взаимодействует с ним

## 📱 Основные команды

В группе или личке с ботом:

```
/today - Ваш чек-лист с кнопками
/all - Прогресс всех участников
/stats - Ваша статистика и streak
/help - Справка
```

## 🔍 Полезные команды

```bash
# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Проверить статус
docker-compose ps
```

## ❓ Частые вопросы

**Q: Нужно ли получать личные Telegram ID?**
A: Нет! Бот автоматически регистрирует всех кто использует команды.

**Q: Можно ли использовать без группы?**
A: Да! Просто оставьте `GROUP_CHAT_ID=0` и общайтесь с ботом в личку.

**Q: Как добавить еще одного участника?**
A: Просто добавьте их в группу, они автоматически зарегистрируются при первом использовании `/today`.

**Q: Бот не присылает напоминания в группу?**
A: Убедитесь что вы настроили `GROUP_CHAT_ID` в файле `.env` и перезапустили бота.

---

**Начинайте работу! 🚀**

📖 Подробная инструкция: [README.md](README.md)
